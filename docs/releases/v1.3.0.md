# SessionGuard 1.3.0

Packaging and distribution hardening release focused on making the direct-download setup path easier to verify and safer to use.

## Highlights

- published SHA256 checksum assets for setup, app, service, and source downloads
- added extracted-bundle verification through `Verify-SessionGuard.ps1`
- updated the combined installer to verify bundle integrity before install by default
- recorded current binary signature status in publish and release manifests

## Included in this release

- new `scripts/install/Verify-SessionGuardBundle.ps1`
- new root-level `Verify-SessionGuard.ps1` inside the published setup bundle
- bundle integrity inventory at `bundle-integrity.json`
- expanded `release-assets.json` with file sizes, SHA256 hashes, and published-component trust metadata
- new `sessionguard-win11-sha256-<version>.txt` release asset
- updated README, getting-started, runtime-model, development, limitations, manual-validation, and roadmap docs

## Architecture

- the product runtime model stays the same: service-backed monitoring with a tray-first desktop shell
- trust reporting is now part of the publish pipeline rather than an external ad hoc step
- setup verification checks extracted files against the publisher-generated inventory, while release checksums cover the downloaded archives themselves

## Limitations

- SessionGuard direct-download binaries may still be unsigned and can still trigger SmartScreen or other Windows trust prompts
- extracted-bundle verification helps detect corruption or local tampering, but the published setup zip hash remains the stronger download-integrity check
- this release improves trust and install ergonomics, not Windows restart coverage itself

## Verification

- `dotnet build SessionGuard.sln`
- `dotnet test SessionGuard.sln`
- `powershell -ExecutionPolicy Bypass -File scripts/ui/Run-UiSmoke.ps1`
- `powershell -ExecutionPolicy Bypass -File scripts/ci/Invoke-WindowsValidation.ps1`
- `powershell -ExecutionPolicy Bypass -File scripts/ci/Invoke-WindowsValidation.ps1 -Configuration Release`
- `powershell -ExecutionPolicy Bypass -File scripts/release/Publish-SessionGuardReleaseAssets.ps1 -Version 1.3.0 -Configuration Release -Runtime win-x64 -SelfContained`
- fresh machine-wide install, reinstall, and uninstall validation from the published `1.3.0` setup bundle
