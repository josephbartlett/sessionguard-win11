# SessionGuard 1.2.0

Tray-workflow refinement release focused on making the tray menu the default day-to-day control surface while keeping the full dashboard and elevated write path clear when you need them.

## Highlights

- refined the tray menu into a shorter daily workflow with status, next step, one common action, and a smaller support area
- added `Open elevated controls` as the explicit path for service-backed write actions when the current app session is still read-only
- aligned tray, dashboard, and notification wording around the same service-versus-write-access model
- added a `--technical-view` startup path so the elevated SessionGuard window can open directly into the detailed control surface without creating another tray shell
- hardened the installed service boundary with trusted pipe access, bounded IPC payloads, and install-user-scoped local state access

## Included in this release

- updated tray menu layout in `SessionGuard.App`
- updated operator alert wording and transition messaging in `SessionGuard.Core`
- updated dashboard guidance and technical-view actions in `SessionGuard.App`
- bounded pipe-message reads to a maximum payload size and added request/response timeouts for the service control plane
- added trusted-server verification in the named-pipe client path before the app accepts service responses
- stamped the installing user SID into published install manifests and used it to scope installed service control-plane access plus installed `logs/` and `state/` directories
- added tests for oversized IPC payload rejection, install-manifest authorization stamping, and runtime access-policy behavior
- updated the bundled setup README plus operator docs in:
  - `README.md`
  - `docs/getting-started.md`
  - `docs/runtime-model.md`
  - `docs/limitations.md`
  - `docs/manual-validation.md`
- updated `docs/plans/v1.2.0-everyday-tray-workflow-plan.md` with the canonical action map

## Architecture

- `SessionGuard.Service` still owns background monitoring and state-changing actions
- `SessionGuard.App` remains the tray and dashboard shell, but the tray menu is now more intentionally the primary day-to-day workflow
- the elevated controls path stays inside the same app/runtime model by launching a separate elevated no-tray SessionGuard window
- installed service control-plane access is now scoped to the installing user, administrators, and `SYSTEM` instead of all authenticated local users
- the pipe protocol now enforces bounded message sizes and short-lived request handling instead of trusting indefinite client reads

## Limitations

- service-backed writes still require both service connectivity and an elevated SessionGuard window
- the tray shell is calmer, but deep diagnosis still depends on opening the main window
- SessionGuard remains a direct-download Windows utility rather than a Microsoft Store package
- in installed mode, reinstalling from a different Windows account changes which user can directly use the installed service control plane and read installed `logs/` plus `state/`

## Verification

- `dotnet build SessionGuard.sln`
- `dotnet test SessionGuard.sln`
- `powershell -ExecutionPolicy Bypass -File scripts/ui/Run-UiSmoke.ps1`
- `powershell -ExecutionPolicy Bypass -File scripts/ci/Invoke-WindowsValidation.ps1`
- `powershell -ExecutionPolicy Bypass -File scripts/ci/Invoke-WindowsValidation.ps1 -Configuration Release`
- `powershell -ExecutionPolicy Bypass -File scripts/release/Publish-SessionGuardReleaseAssets.ps1 -Version 1.2.0 -Configuration Release -Runtime win-x64 -SelfContained`
- fresh installed-mode validation from `artifacts\releases\1.2.0\publish\SessionGuard\Install-SessionGuard.ps1`
- live reinstall validation over an already running installed copy
- full uninstall validation from `C:\Program Files\SessionGuard\Uninstall-SessionGuard.ps1 -RemoveFiles`
